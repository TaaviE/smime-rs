{
  "name": "rust-smime-demo",
  "version": "0.1.12",
  "private": true,
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "wasm-build": "cd .. && wasm-pack build --release --scope zone-forge -- --no-default-features --features console_error_panic_hook && cp -r src/locales pkg/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.5.0"
  }
}
